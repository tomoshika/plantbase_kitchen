<div class="new-comment mt-3">
  <h5>コメント投稿はこちら</h5>
  <%= form_with model:[recipe, comment], remote: true do |f| %>
    <%= f.text_area :recipe_comment, placeholder:"コメント入力", class: "form-control mb-2" %>
    <%= f.submit "送信" %>
  <% end %>
</div>
